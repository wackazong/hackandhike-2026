import { Theme } from "generated-theme.slint";

// Dynamic log text is rendered directly by CPU0 from the fixed PSRAM snapshot.
// Slint owns only the static backing surface and navigation.
export component LogView inherits Rectangle {
    background: Theme.white;
}
