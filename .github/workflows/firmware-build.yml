name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  HOST_RUST_VERSION: "1.97.0"
  XTENSA_RUST_VERSION: "1.97.0.0"

jobs:
  host-tests:
    name: Host tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Install host Rust toolchain
        run: rustup toolchain install "$HOST_RUST_VERSION" --profile minimal --component clippy

      - name: Lint the core crate and its tests
        run: |
          host_target="$(rustc +"$HOST_RUST_VERSION" -vV | sed -n 's/^host: //p')"
          cargo +"$HOST_RUST_VERSION" clippy -p hack-and-hike-core --all-targets --target "$host_target" -- -D warnings

      - name: Run host tests
        run: RUSTFLAGS="-D warnings" ./scripts/test.sh

  firmware:
    name: Firmware lint and build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Install Xtensa Rust toolchain
        uses: esp-rs/xtensa-toolchain@v1.7
        with:
          default: true
          buildtargets: esp32s3
          version: ${{ env.XTENSA_RUST_VERSION }}
          ldproxy: false

      - name: Cache Cargo registry and build tree
        uses: Swatinem/rust-cache@v2

      - name: Check formatting
        run: cargo fmt --check

      - name: Clippy (library and every application binary)
        run: cargo clippy --locked --release -- -D warnings

      - name: Build every application binary
        run: cargo build --locked --release
